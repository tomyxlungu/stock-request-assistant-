import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'srm.tomyxtech.stockrequestassistant',
  appName: 'stock-request-assistant',
  webDir: 'dist'
};

export default config;
